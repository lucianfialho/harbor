/**
 * HubSpotClient — advanced Effect HttpClient service (future use).
 * For now, ContactsDestination uses direct fetch for simplicity.
 * This file is intentionally minimal — full implementation in a future PRP.
 */
export {}
